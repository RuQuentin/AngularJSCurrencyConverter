'use strict';

function transactionsListController($rootScope) {
  'ngInject';
  
  // eslint-disable-next-line no-console
  console.log($rootScope.currentUserDeals);
}

export default transactionsListController;